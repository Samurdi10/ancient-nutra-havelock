VITE_SUPABASE_URL=https://your-multix-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-multix-anon-key

# QuickBooks Online OAuth (Client ID is public, unlike the Client Secret which
# only ever lives as a Supabase function secret — see README's QuickBooks section).
VITE_QBO_CLIENT_ID=your-intuit-app-client-id
VITE_QBO_ENVIRONMENT=sandbox
